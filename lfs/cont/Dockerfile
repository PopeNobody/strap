FROM ubuntu
WORKDIR /boot/strap
VOLUME /boot/strap:/boot/strap

RUN ln -sf bash /bin/sh
RUN /bin/pwd > pwd.out
# RUN /bin/bash lfs/install.sh

# RUN apt-get update -y
# RUN apt-get upgrade -y
# 
# RUN apt-cache pkgnames > pkgnames.init
# RUN apt-get update
# RUN apt-get install
